BOS v2.1.3 — IR/BOS branded, complete schema (boot reconcile), Traefik auto-detect, COO runtime map. + Installer now creates your owner account with YOUR email as the login (prompted at setup), no generic admin@. Install: bash deploy/install.sh --pwd <password>
