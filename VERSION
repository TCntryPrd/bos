BOS v2.1.2 — IR/BOS branded, complete schema (boot reconcile), Traefik auto-detect. Fixes: boss_bash cwd, Gemini tool schemas, CLI 2.1.185, + COO runtime/access map (orb no longer rediscovers its container limits each turn). Install: bash deploy/install.sh --pwd <password>
